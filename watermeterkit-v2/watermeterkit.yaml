# WaterMeterKit V2 Configuration
# Main entry point for V2 firmware (WiFi only)

substitutions:
  default_network: "WiFi"

packages:
  base: !include base.yaml
  wifi: !include wifi.yaml

dashboard_import:
  package_import_url: github://smarthomeshop/watermeterkit/watermeterkit-v2/watermeterkit.yaml@main
