# WaterMeterKit V1 WiFi Configuration (Legacy URL for backwards compatibility)
# New users should use: github://smarthomeshop/watermeterkit/watermeterkit-v1/watermeterkit-wifi.yaml@main
#
# This file redirects existing users to the new v1 structure via OTA update.
# After OTA, users will automatically use the new modular YAML structure.

packages:
  base: !include watermeterkit-v1/base.yaml
  wifi: !include watermeterkit-v1/wifi.yaml

dashboard_import:
  package_import_url: github://smarthomeshop/watermeterkit/watermeterkit-v1/watermeterkit-wifi.yaml@main
